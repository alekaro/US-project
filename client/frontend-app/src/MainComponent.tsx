import { useCallback } from "react";

const MainComponent = () => {
    // const getAllNumbers = useCallback(() => {});

    return (
        <div>
            <p>Jebać PiS</p>
        </div>
    )
}

export default MainComponent;